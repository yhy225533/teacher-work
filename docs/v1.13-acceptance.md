# V1.13 验收记录（课件区材料分组：lesson_files role 列 + 启发式 + 手动改组）

- 日期：2026-09-13
- 设计基准：`docs/v1.13-lesson-material-groups-plan.md`；决策 D74–D77（`implementation-tasks/V1_13_DECISIONS.md`）
- 任务链：`implementation-tasks/v1.13-tasks/`（V113-A → B → C）；基线 `plan(V1.13)` 提交 `29da17f`
- 唯一全量验收点：V113-C（本文档）

## 1. 交付内容

| 节点 | 内容 | 提交 |
|---|---|---|
| V113-A | `LessonMaterialGroup` 合同/守卫 + `ManagedFileLink.role?` + `files:set-material-group` 通道 + migration v18（lesson_files 纯 ADD COLUMN role，四值/NULL CHECK）+ `setLessonMaterialGroup` 服务 + preload + contentChanged 补发 | `6fdc346` |
| V113-B | `lessonMaterialGroupRole` 七条首中即停启发式（含「补充讲义→讲义」产品裁决）+ 四组树渲染 + 树行 ⋯ 改组菜单（恢复自动/当前组置灰/版本链不出菜单）+ section/hero 接线 | `8e4104d` |
| V113-C | 全量门禁 + 隔离 Windows 冒烟 13/13 + 本验收文档 | 本文提交 |

## 2. 自动质量门（V113-C 实测）

- 全量测试：**100 files / 592 tests passed（1 skipped 既有）**；
- `npm run typecheck`：通过；
- `npm run lint`：通过；
- production build：通过（pdf.worker 等资产正常产出）；
- `git diff --check`：通过（无空白错误）。

如实记录：全量套件首轮曾出现 `backup-restore.test.ts` 1 例失败（Windows 临时目录并发争用），单跑两次均通过、全量复跑通过；V1.13 未触碰备份代码，判定为既有偶发，与本版无关。

migration 钉测演进（17→18 标准演进，先例 v17 当年 16→17）：`workspace-foundation` / `v17-a-migration` / `v1.2-core-progress` / `v1.3-course-setup-core` / `v1.1-acceptance` / `v1.3-acceptance` / `mineru-migration`（另补 v15 合成夹具缺失的 lesson_files 表，与真实 v15 schema 对齐）。

## 3. 隔离 Windows 冒烟（13/13）

框架沿用 V111-D/V112-C（production `out/main/index.js` + 隔离 `TEACHER_WORKBENCH_L01_SMOKE_APP_DATA` + `--user-data-dir` + DevTools 协议）。脚本 `tmp/v113-smoke/run-smoke.mjs`。

| # | 场景 | 断言 |
|---|---|---|
| 1 | fixture 五文件（版本链 md/讲义 docx/练习 pdf/试卷 doc/随记 md）+ 外部根预登记 | 全部就绪 |
| 2 | 课程 + 课次 + 5 文件挂课（白名单 copyToLesson） | 全部入库 |
| 3 | 课程页行动卡 chips | `📘 讲义 2` / `📎 材料 3`（D76 新语义：导入讲义计入讲义侧） |
| 4 | 课件区四组渲染 + 启发式归组 | 讲义[版本链 md + 讲义 docx] / 习题与作业[练习 pdf] / 试卷与复习[试卷 doc] / 其他资料[随记 md]，组序正确 |
| 5 | D46 零回归 | 版本链文件"当前"徽标在、不出改组菜单；杂项文件出菜单 |
| 6 | 手动改组 | 菜单选「试卷与复习」→ 随记即时移入目标组（contentChanged → reload 链） |
| 7 | 持久化（D74） | `lesson_files.role = 'exam'` 直查 DB 证实 |
| 8 | 菜单状态 | 当前组项 disabled、恢复自动可用、其他组可选 |
| 9 | 恢复自动 | 随记回其他资料组；`role` 回 NULL（自动启发式） |
| 10 | 阅读链零回归（V111-C） | 讲义 docx 应用内预览渲染正文 |
| 11 | stderr 健康检查 | 无 Unhandled / crash |

冒烟脚本自身缺陷如实记录（两处，均已修复并复跑）：①docx 夹具生成器 local header 越界写 body 被截断 + central directory 缺版本字段 + offset 漏加 body 长度 → jszip 拒载（诊断：独立 jszip 探针）；②四组断言首版对显示文本的匹配条件未随 `displayFileName` 行为对齐 → 改逐条件布尔输出。产品代码无缺陷。

## 4. 范围红线核对

- D46 冻结语义零变化：`isLessonLectureFile` / `classifyLessonCoursewareFiles` / `setLessonFileRole` / 提讲义入口可见性原样（钉测演进仅为 D76 记录在案的分组实现与文案变化）；
- migration 仅 v18 一个纯 ADD COLUMN，无表重建（v16 事故守卫框架照用）；
- 零新依赖；新 IPC 仅 `files:set-material-group` 一条（成功补发既有 contentChanged，V110-A/D61 先例）；
- 编辑器/导出链/反馈链/题库链/外部资料预览（D70–D72）零改动；student_files 零改动；
- 导入路径（importToLesson/copyAndRegister/外部 copy-to-lesson）零改动——新行 role 为 NULL 自动归组（D74 不回填）。

## 5. 走查清单（产品负责人确认后创建 `checkpoint-V1.13-pass`）

1. 课件区四组显示与组序（📘 讲义 → ✏️ 习题与作业 → 📄 试卷与复习 → 📎 其他资料）；
2. 导入的讲义类文件（含「补充讲义」系列）自动落入讲义组；习题/作业、试卷/复习、杂项各归其位；
3. 任一非讲义文件 ⋯ → 改组 → 组间移动即时生效，重启应用后保持；
4. 「恢复自动分组」回到启发式结果；
5. 版本链讲义（当前/历史）不被改组菜单触碰，"当前"徽标正常；
6. 课程页行动卡 讲义/材料 计数与四组一致；
7. 抽查一课的 docx/pdf 应用内预览正常（V1.11 链零回归）。

## 6. 已知限制与后续候选

- 启发式识别错的文件需手动改组（不追求规则完美；规则表冻结于 D75，改进需新版本）；
- `misc`（其他资料）组内不再细分（英语词汇/课本定义等）；
- 学生资料（student_files）无分组维度；
- 学生版/教师版第二维度标签、MD 正文本地链接可点击、导入脚本改进（反馈 md 不入库等，见方案 §6）均为后续候选。
