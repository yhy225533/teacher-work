# V1.7.3 · MD 编辑器公式输入与布局改版验收记录

**结论（2026-09-05）：** V173-A–V173-D 实施完成并冻结于各自本地提交；V173-E 自动门全部通过（全量测试、typecheck、lint、production build、`git diff --check`、隔离 Windows 冒烟）。产品负责人按方案 §9 走查清单真实窗口确认**待执行**（清单见文末），确认通过后才创建 `checkpoint-V1.7.3-pass`。

## 一、交付内容（对应方案 §8 任务链）

| 里程碑 | 提交 | 交付 |
|---|---|---|
| V173-A | `v1.7.3(V173-A): math mode engine and symbol bar` | `math-input.ts` 纯函数引擎 + 32 项缩写表 + 数学模式侦测/最长匹配/自动分式/三级槽位跳转 + textarea 接线 + 数学态符号条（速查面板退役） |
| V173-B | `v1.7.3(V173-B): slash command menu and formula hotkeys` | 空行 `/`/`、` 斜杠菜单（拼音过滤 + 全键盘导航 + 占位符光标）+ Ctrl+M / Ctrl+Shift+M 公式快捷键 |
| V173-C | `v1.7.3(V173-C): view modes, draggable split and sync scroll` | ✎/◫/👁 三视图（按文件记忆）+ 0.25–0.80 可拖分栏 + 行比例单向同步滚动（800ms 反向抑制） |
| V173-D | `v1.7.3(V173-D): math cache, error fallback and preview debounce` | MathSpan 公式 LRU 缓存（300 条）+ KaTeX 错误红色降级 + 预览 120ms 防抖 |

## 二、自动门证据（2026-09-05）

- **全量测试**：79 files / 395 tests passed（394 passed + 1 skipped 既有真实题库冒烟）；新增 `tests/v1.7.3-formula-ux.test.ts` 21 例（纯函数 9 + 源码接线 8 + 渲染 3 + 防抖 1）；`v17-c-md-editor.test.ts` 速查钉测演进为缩写表/符号条断言。
- **typecheck / lint**：通过（`--max-warnings 0` 语义不变）。
- **production build**：main 420 kB / preload 50 kB / renderer 1.54 MB（含 KaTeX 字体资产），三进程构建成功。
- **`git diff --check`**：通过。

## 三、隔离 Windows 冒烟（2026-09-05）

独立 `TEACHER_WORKBENCH_L01_SMOKE_APP_DATA` 临时目录与 `--user-data-dir` 启动 production Electron（out/main/index.js）：

- 4 进程（主/GPU/utility/renderer）存活；隔离 `TeacherWorkspace` 下 `workspace.db`（WAL/SHM）成功创建；
- 窗口稳定呈现、a11y 树可交互（textarea 聚焦正常）、stderr 无错误（仅 npm 环境提示行）；
- 冒烟库直接验证：`schema_migrations` 应用 1–17、`notes` CHECK 含 `manual_edit`（V1.7 语义未被 V1.7.3 改动）；
- 未接触正式工作区、真实教学资料或任何 API Key；冒烟后 4 进程全部终止、临时目录已删除。

## 四、测试内覆盖（中继式验收）

- 数学模式侦测：`$$` 按 token 翻转（块级/行内统一）、偶数 `$` 闭合、价格句 `$5…$7` 误报防护；
- 缩写：最长优先（`angle` 不被 `le` 抢占）、词边界（`limx` 不展开）、光标落首个空 `{}`；自动分式分子已填让位分母；
- Tab 三级跳转（空 `{}` → `}` → `$`/`$$`）、无可跳不劫持；
- 斜杠：拼音/中文/英文 startsWith 过滤、占位符剥除与槽位优先、顿号改写触发、菜单键盘导航、blur 关闭；
- 三视图：记忆读写与非法回退、拖动钳制与持久化、单向同步与反向抑制窗；
- 渲染管线：内容缓存等值复用、坏公式红色 `math-error`（⚠ + KaTeX 消息 + title 原串）代替整段回源码、预览防抖与保存/热保存路径分离（`bodyMd: body` 钉死）。

## 五、安全边界复核（V1.7.3 重点）

- 零新依赖（无 codemirror/monaco/prosemirror，钉测）、零新 IPC、零 migration、零 Main 改动；
- 只改 renderer 三文件（`math-input.ts` 新增 + `md-editor.tsx` + `lesson-material-reader.tsx` MathSpan 局部）+ `styles.css` + 测试；
- 保存语义不动：`files:write-version` 永写新文件、版本链/外部编辑版命名、原件只读（V17-C/V17-A 钉测全部保持通过）；
- `parseBlocks`/`renderInline` 结构未动，仅 `MathSpan` 渲染路径加缓存与降级；
- 未运行 portable/installer；未引入可执行 snippet（纯数据表）；未做编辑区内嵌 WYSIWYG（D38 否决项均未触碰）。

## 六、限制与遗留（候选后续增量，见 D37/D38）

- 同步滚动为行比例近似版（表格/公式密集文档有漂移），块锚点版留后续；
- 沉浸模式（编辑器铺满 + 左侧栏整体隐藏）为产品负责人 2026-09-05 指定的后期增量，本版本仅保证编辑器布局不写死依赖侧栏；
- 公式悬浮预览浮窗、KaTeX 宏配置未排期（D38 非目标）；
- 空格展开触发键为常量可切换（体验反馈不佳可整体换 Tab，引擎与数据表不变）。

## 七、产品负责人走查清单（方案 §9，确认后创建 `checkpoint-V1.7.3-pass`）

1. 空公式内输入 `frac`+空格 → `\frac{}{}` 光标在分子；`sqrt`/`sum`/`int`/`cases` 同验；
2. 公式内 `x` 后按 `/` → `\frac{x}{}` 光标在分母；正文英文 `x/` 不触发；
3. Tab 依次跳空槽位、跳出 `}`、跳出 `$`；正文 Tab 不被劫持；
4. 空行 `/` 弹菜单，`gs` 或 `公` 过滤，Enter 插入且光标在 `$$` 中间；`、` 同验；Esc 关闭；非空行 `/` 正常输入；
5. Ctrl+M 块级 / Ctrl+Shift+M 行内（选中文字包裹）；
6. 三视图切换 + 分栏拖动 + 刷新后按文件记忆恢复；编辑侧滚动预览单向跟随；
7. 300 行含 20 公式课件连续输入流畅；坏公式红色错误消息（非整段回源码）；
8. 撤销 ↶ 可回退展开/分式/斜杠插入；保存为新版本语义与之前完全一致。
