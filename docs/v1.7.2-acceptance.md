# V1.7.2 验收记录 · 备课工作台结构重排（D33/D34，D35 修订）

- 基线：`checkpoint-V1.6-pass` 之上的纯展示层增量（零逻辑/合同/IPC/依赖/migration 变化）。
- 设计基准：`docs/v1.7.2-prep-workspace-restructure-plan.md`；决策 D33/D34（`implementation-tasks/V1_7_DECISIONS.md`）+ D35（目标题数可选可填、上限 80）。
- 任务链：`implementation-tasks/v1.7.2-tasks/` V172-A → V172-B → V172-C → V172-D（本文件）。

## 一、交付内容（对照方案 §5）

| 区块 | 落地情况 |
|---|---|
| 左轨 `prep-rail`（250px） | V172-A：修改记录时间线（三模式同构）+ 空态文案 + 脚注"AI 结果永不覆盖原件" |
| 生成器卡一行一类 | V172-A：范围行（single 目标卡/更换展开收起、lesson 自动徽标）；V172-B 前已提前落地参考行 chips/三入口/题库开关/预算 meter |
| new 冷启动大卡 | V172-B：0 选中 → `prep-add-cards` 两张虚线大卡（从外部资料添加/从素材库添加）+ "已选 0 份 · 也可以只靠要求直接生成" + （有候选时）"＋ 从本课资料选择" + 题库开关；有选中 → chips + 三个小入口 + 开关（与参考行同构） |
| 题库参数迁移 | V172-B：`PrepBankOptions`（目标题数 number input + datalist 快捷项 1..80、学生版开关）唯一实例迁至题库候选区头部 `prep-bank-controls`（与"正在选题…"同处）；参考行/生成依据行内过渡态删除；`prep-bank-toggle`/`prep-bank-options` CSS 退役 |
| 收起态 | V172-A：`✦ 修改对象/修改范围/生成依据 … · 要求「前 18 字…」 · 参考 n 份 · 题库{开|关}` + [调整要求]；规则 1-5（选中自动收起、取消强制展开、审阅/流式保持展开）；V172-C 补钉测防回归 |
| 主区空态 | V172-C：`workspace-card draft-content-empty` 卡片化 + 分模式文案（new："先添加生成依据（或直接写要求），点「生成讲义」即可出第一版…"；single/lesson："修改方案生成后先在这里审阅，确认后才会生成新副本…"）；旧通用文案退役 |
| `improve-review-card` | V172-C 核查：自带独立边框与内边距（卡片视觉齐备），不叠加 `workspace-card`，钉测锁视觉 |
| 响应式 | 1100px 断点单列堆叠（左轨在上、rail 取消 sticky）——V172-A 改写媒体查询，V172-D 冒烟验证 |

## 二、自动门（2026-09-07）

- 全量 `npm test`：**83 files / 462 tests passed（1 skipped 既有）**；
- `npm run typecheck` ✅；`npm run lint` ✅；`npm run build`（electron-vite 三端）✅；`git diff --check` ✅；
- 未运行 portable/installer（符合约束）。
- 钉测演进记录（用户可见结构/文案随 §8 对照表演进；逻辑类断言零改动）：
  - `tests/v1.7.2-workspace-structure.test.ts` 15 例（V172-B 新增 2 例：候选区头部唯一实例、冷启动大卡/chips 同构；V172-C 追加 3 例：分模式空态、review-card 独立视觉、收起/展开规则）；
  - 入口防再犯钉（V172-fix）2 → 3 处（新增冷启动大卡入口）；
  - `static-render-v156-d.test.ts` 加载态断言按真实行为演进（`files === null` 时仅"正在读取本次资料…"，大卡在资料就绪后渲染）。

## 三、隔离 Windows 冒烟（2026-09-07，脚本 `tmp/v172-smoke/run-smoke.mjs`，本地不入库）

production `out/main/index.js` + 独立 `TEACHER_WORKBENCH_L01_SMOKE_APP_DATA` + `--user-data-dir` + 本机 `127.0.0.1:18771` fake OpenAI-compatible 服务（SSE 流式按 V16-C 合同分流）+ DevTools 协议驱动，默认安全配置，**16/16 通过**：

1. 外部资料根目录预登记（隔离库 `external_roots` singleton 行；`chooseRoot` 白名单通道需系统对话框无法自动化，此为同一张表的等价单行写入，冒烟库专用）；
2. 测试数据创建：课程 + 两课次 + 学生 + 外部 md 经白名单 `externalLibrary.copyToLesson` 复制进课次；
3. 真实 UI 导航：搜索↔课程切换 + 页面"刷新"（共享 overview 缓存不随后建数据自动刷新）→ 课程卡"开始备课" → 备课工作台（课内有 md → single 模式）；
4. 左轨（修改记录 + 脚注）、目标卡（文件名/字数/附注；"当前版"徽标与"更换"按钮为条件渲染，单文件课次不显示，断言按条件语义校验）、参考行（chips 入口 + 参考题库开关）、预算行（`已用 n / 30,000 字 · 参考 n / 10 份`）、single 空态分模式文案卡片化；
5. fake AI（流式 SSE）生成第一版讲义并经 `drafts.publishToLesson` 发布 → `有理数混合运算 · 第 1 版.md` 落到课次；
6. 退出备课再重进（DraftPanel 重挂载拉取新节点）→ 左轨出现修改节点 → 点击节点生成器自动收起（摘要 `✦ 修改对象 … · 要求（未填写） · 参考 0 份 · 题库关` + [调整要求]）→ 点"调整要求"重新展开；
7. 未安装题库：开关置灰 + title"先在题库页导入 .tqbank"；生成器内无"目标题数"（参数只属于候选区头部）；
8. 视口 1000px（主窗口 `minWidth: 960` 物理上无法压到断点内 → DevTools `Emulation.setDeviceMetricsOverride` 仿真）：`lesson-prep-workspace-grid` 单列、左轨在上、rail `position: static`；
9. fake provider 收到讲义生成请求（含课次上下文）；stderr 无致命错误；冒烟后 Electron 进程全部终止、临时目录删除（tasklist/TEMP 双复核）。

## 四、安全边界复核

- 零新依赖、零 migration、零新 IPC、零 Main/合同改动（D34 非目标全部未触碰）；
- 只改 `draft-panel.tsx` 展示层 + `styles.css` + 测试；生成/修改/题库/预算/发布的全部 handler、IPC、Main 逻辑零改动（既有逻辑类钉测全绿）；
- 不动 DraftInbox、teaching-content-page 导航、lesson-files-section；
- 冒烟隔离目录（app-data/external-root 均为合成内容），未接触正式工作区、真实教学资料或任何 API Key。

## 五、产品负责人走查清单（通过后创建 `checkpoint-V1.7.2-pass`）

1. 无资料新课次 → 新建备课冷启动两张大卡 → 添加后 chips → 生成讲义出第一版；
2. 有 md 课次 → 单文件：目标卡/更换展开收起、参考 chips 增删与"＋本课资料"、预算行、要求→方案→确认→生成→新旧对比→发布全流程；
3. 整课重做：范围徽标、题库开关打开 → 候选区头部改目标题数/勾学生版 → 剔除/调整重选 → 确认生成；
4. 左轨记录切换、草稿删除、收起态"调整要求"展开再收起；
5. 外部资料/素材库 picker 往返返回正常；
6. 窗口缩到 1100px 以下不破版（注意：主窗口最小宽度 960，实际需要外接窄窗或开发工具仿真验证）。

> 说明：本增量纯展示层；真实 DeepSeek 自测按 V1.7 自测清单另行执行（V1.7.2 未改 AI 逻辑，无需单独真实自测）。
