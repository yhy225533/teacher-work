# V1.9 决策记录（课件导出可打印 PDF + 教学界面信息密度收口）

产品负责人 2026-09-07 确认 PDF 导出草案（`docs/pdf-export-plan-draft.md`，已转正为 `docs/v1.9-pdf-export-plan.md`）并立项；同日三轮界面评审（备课工作台 / 课件区 / 课程页 / 编辑器评估）拍板 UI 重做并入 V1.9，设计基准 `docs/v1.9-teaching-ui-restructure-plan.md`。交互示意 `tmp/mockups/`（pdf-export-flow / prep-workspace-dialogue-v2 / courseware-actions-cleanup / current-lesson-first-landing，本地不入库）。PDF 导出决策点 D48–D53 按推荐冻结、非目标 D54；UI 重做决策 D55–D60。

## D48–D54 · PDF 导出（见 `docs/v1.9-pdf-export-plan.md` §10；详细内容随原草案转正保留如下）

## D48 · 导出范围 = 课次内全部 md（与 D27 同宽）

- 入口为课件区阅读器操作行「导出 PDF」，仅选中 `text/markdown` 时显示；
- 版本链任意版本（当前版/历史版）、外部导入 md、编辑版 `（编辑版）.md`、学生版 `… · 学生版.md` 均可导出；
- 与 D27（全部 md 可 AI 二改）同宽：导出是只读动作，风险低于修改；Office/PDF/图片文件不提供入口（本就可「系统打开」打印）。

## D49 · 保存交互 = 原生另存为对话框，成功后自动「在文件夹中显示」

- 原生 `showSaveDialog`，默认文件名 = 原文件名 `.md`→`.pdf`（学生版等长名同样只换扩展名）；
- 落盘 = 同目录临时文件 + 原子重命名（沿用 managed 写入先例），目标被占用/失败返回稳定错误并清理，绝不留半成品；
- 保存成功后 Main 侧 `shell.showItemInFolder` 自动定位产物；响应只回传 `{ saved: true|false }`，**不回传路径**（守 V11-01"绝对路径不进 Preload 响应"）。

## D50 · 页眉页脚 = 左「学生名 · 课次标题」+ 右导出日期 / 居中页码

- `printToPDF` 的 `headerTemplate`/`footerTemplate` 实现：页眉左 = headerText（Renderer 拼显示串，≤100 字，缺省时省略不报错）、右 = 导出日期；页脚居中「第 X 页 / 共 Y 页」；
- 9px 灰字；Chromium 模板仅支持系统字体与内联样式，不做个性化。

## D51 · 版式 = A4 纵向、11pt、白底黑字省墨、防断裂规则

- A4 纵向，margins 上下 16mm / 左右 14mm；`printBackground:false`；
- 正文约 11pt（走查后可调），中文走系统字体栈（微软雅黑）；
- 防断裂：显示公式块、图片、表格、代码块、引用 `break-inside: avoid`；标题 `break-after: avoid`；
- 版式 CSS 独立文件 `print-document.css`，仅打印窗加载，主窗样式零改动。

## D52 · 多文件合并成册 = 候选，本期不做

- 教师版+学生版合并一份 PDF、多讲义按序合并成册，均记为后续候选；
- 本期一次导出一份文件；导出流程单导出串行（并发第二请求 `EXPORT_BUSY`）。

## D53 · 首页不做封面式标题块

- 文档自带 H1，不自动加封面页/标题块；如需封面记为候选。

## D54 · V1.9 非目标与边界

- 零新依赖、零 migration、零 AI 调用（导出不花钱）；
- 不在 Main 重写 md→HTML 解析器（否决理由：与 Renderer 渲染双实现必然不一致）；不改动 `MarkdownDocument` 既有解析与渲染语义（打印视图只消费）；
- 导出 PDF 不登记 `files` 表、不进搜索索引、不进备份包——它是打印产物，不是教学资料；原课件文件字节不动；
- 不做 Office/PDF/图片转 PDF、批量静默导出整学期、docx 导出（D26 已否决）、云端转换、PDF 编辑/水印、页眉页脚可视化编辑器；
- 新 IPC 仅 `export:print-to-pdf` / `export:get-print-payload` / `export:print-ready` 三条（载荷通道带 sender 校验）；不运行 portable/installer。

## D55 · 备课工作台 = 对话式重做（撤模式切换器与固定工作流）

- 背景（2026-09-07 原话）："右边这个配置应该是指我和 AI 进行对话……其实只有在内容是全新的或者为空的时候，才会是整课重做。但凡有东西，本质都是 AI 修改，哪怕你觉得完全不行，你也会跟 AI 说这个东西完全不行，我们要整个进行重做。""不是所有的备课都是同一种工作流，我们没有办法去判断这个工作流一定是这样的。"
- **右栏 = 与 AI 对话**（依据区 + "对 AI 说" + 发送）：课次有 md → 自动挂当前讲义（AI 修改）；空课次 → 从零生成；"整课重做"= 要求中的一句话 + 依据区"整个课件包"选项，不再是独立模式按钮。合同层 `prepMode`/`draft:generate` 的 `mode` 字段保留，仅 UI 收敛（自动初始化 single/new，"整个课件包"映射 lesson）。
- **主区 = 单一舞台**，固定位置同卡替换：方案（题库候选与确认按钮就地并入）/ 生成中（流式同卡，页面不跳）/ 成果 / 对比；**不编号、不步骤链**——标题只陈述"现在是什么"，任意状态间跳转允许。
- 病灶量化：现状主列竖排 5-6 区、一次改讲义竖穿 4 张卡、题库开关与候选隔 2 张卡、成果 7 键平权、成果阅读区剩 2-3 行。
- 独立题库候选卡与独立确认条撤除（并入方案态）；预算行简化（D25 确认逻辑不动）。

## D56 · 修改记录默认收起 + 成果编辑复用 MdEditor

- 背景（原话）："修改记录就这么重要吗？就一定放在最左边……占的篇幅有点太大了，他应该是要么不放在这，要么可以收回去隐藏。"
- 修改记录 rail 默认收起（0 占版面）→ 顶部「🕘 修改记录 N」按钮 + 浮层（复用 TeachingContentDrawer 视觉模式）；选择即加载、关闭即还原。多轮回看能力保留。
- **成果态 ✎ 编辑复用 MdEditor**（消灭两套 md 编辑体验——现状：课件区 MdEditor vs 备课工作台裸 textarea）：`MdEditor` 增可选受控用法（`initialBody` + `onSaveBody`），文件用法（D29 write-version 语义）零变化；note 热保存键 `md-editor-draft:note:<noteId>` 独立命名空间；保存走现行 note 编辑语义（`draft_status` 生命周期零改动）。

## D57 · 课件区双头合并 + 三主键 + ⋯ 收纳（原"方案 B"）

- 病灶：三层头部（页面头 + 本课课件头 + 可读资料头）、11 个平铺按钮（加导出 12）、课次标题同屏重复 3 次、核心信息仅一行小字。
- 合并为单条工具行：课次标题 + 当前文件信息胶囊（文件名 · 当前徽标 · 大小 · 来源标签）｜右侧三主键 `✦ 修改这份`｜`✎ 编辑`｜`⬇ 导出 PDF`（V19-E 接线后渲染）+ 沉浸阅读 + `⋯`。
- 低频/高危动作全进 ⋯（本课：刷新/继续上次修改/整个课件包重做；本文件：设为讲义底稿/系统打开/所在文件夹/MinerU/—红区—从本课移除），共用菜单组件（素材库右键菜单先例）。V1.8.1 分组树/提讲义/历史折叠、阅读与题图跟随语义零变化。

## D58 · 课程页右上方 = 当前课次行动卡（静态名片降为 crumb）

- 背景（原话）："我们本身打开主要是为了知道现在做哪一课、有哪些内容，而不是去看右上方的名字等一大堆没用的信息。"
- 撤 course-page-header 统计条（统计/建课收进行动卡 ⋯"全局"组）；course-detail 头改为**当前课次行动卡**：小字 crumb（课程 · 模式 · 阶段 · 共 N 课）+ 大字当前课次 + chips（排课时间 / 学员 / 讲义 N / 材料 N / 上节反馈✓，全部现成 overview 派生）+ 行动键（进入教学内容 primary / 点名 / 确认本课已上 / ⋯）。
- 选中非当前课次时行动卡主角换为该课次；已结束仅查看 + ⋯；未选课程回退最小页头。今日点名条/课次表/V1.8 反馈链零变化。"打开直落教学内容"记为后续目标态（不动默认导航）。

## D59 · 非连续聊天流（本版边界）

- 对话栏为"对话式呈现"：沿用既有方案→确认→生成合同与提示词体系，生成/题库/发布逻辑零改动；**不持久化对话往返**，每轮产出仍是一个修改节点。真正的连续聊天记录（多轮对话存成一条流、AI 看到全部上文）为后续候选，独立立项。

## D60 · CodeMirror 6 候选不进本期

- 2026-09-07 评估：CodeMirror 6（Live Preview/语法高亮）技术成立，但当前讲义规模（6K-40K 字）textarea 无实际瓶颈；math-input 光标模型与 textarea 深耦合、迁移 = 接口层重写 + Live Preview Widget 全新开发 + V1.7.3 测试重做；且真实备课试跑未做，不应在未验证前重写编辑器底层。
- 保留 math-input.ts 纯函数引擎与全部现有编辑器能力。**触发条件**：真实备课试跑确认"编辑体验"为备课主要摩擦点后，独立立项（候选 V1.10+）。

## 技术事实依据（2026-09-06 代码走读，方案 §2）

1. `MarkdownDocument` 渲染管线全在 Renderer 进程内、纯内存数据（`body + files` → KaTeX 同步 HTML + 图片 data URL），任何窗口可 1:1 复现预览；
2. 图片按 `originalName` 匹配本课文件 → `files:read-content` 返回 data URL，不存在文件路径解析问题；
3. KaTeX 字体（woff2）随包分发，file:// 下正常，打印前等待 `document.fonts.ready`；
4. 窗口安全先例可复用：`windowWebPreferences`（contextIsolation/sandbox）+ `applyWindowNavigationGuard`；
5. 原生对话框注入先例可复用（chooseSourcePath/chooseTranscript/chooseBackupDestination 同模式）；
6. sender 收窄先例可复用（V16-C `extractIpcSender`）。
